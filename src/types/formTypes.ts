export interface RegisterFormType {
  name: String
  email: String
  age: Number
  password: String
  confirm_password: String
  country: String
  tos: Number
}

export interface LoginFormType {
  name: String
  email: String
}
