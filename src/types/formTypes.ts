export interface RegisterFormType {
  name: string
  email: string
  age: Number
  password: string
  confirm_password: string
  country: string
  tos: Number
}

export interface LoginFormType {
  email: string
  password: string
}
